#include <iostream>
#include <cmath>
using namespace std;

/*Commits on Aug 10, 2026*/
using ors = int;
namespace first{
    int x = 10;
}namespace second{
    int x = 15;
}
typedef std::string ors_t;
int main()
{
    
    /*
    //Commits on Aug 9, 2026
    ors_t chalo;
    cout<<"Enter your first word!";
    cin>>chalo;
    cout<<"Here is your first word to start with "<<chalo;

    //Commits on Aug 10, 2026
    ors x = 5;
    cout<<"\n"<<x;
    cout<<"\n"<<second::x;
    cout<<"\n"<<first::x;
    
    //Commits on Aug 11,12 2026
    double y;
    cout<<"\nEnter your marks obtained: ";
    cin>>y;
    string name;
    cout<<"Enter your full name: ";
    getline(cin>>ws,name);
    double per = (y/510) *100;
    cout<<name<<" Got "<<per<<"% in his matriculation";
*/
    //Commits on Aug 13, 2026

    double x = 3.4;
    double y = 5.3;
    double z,n;
    //z = max(x,y);
    //n = min(x,y);
    //n = pow(x,y);
    //z = sqrt(y);
    //n = ceil(x);
    //z = floor(y);
    //n = abs(-34);
    //z = round(y);
    //cout<< "Here is the absolute "<<n<<" Here is the round off result "<<z;
    double a,b;
    cout<<"Enter the first side of the right angled triangle: ";
    cin>>a;
    cout<<"Enter the second side of the right angled triangle: ";
    cin>>b;
    double c =sqrt(a*a + b*b);
    cout<<"Here is the third side: "<<c;



    // Fasih gift center change contacts shorte 
    
}  